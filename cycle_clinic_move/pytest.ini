[pytest]

addopts = --capture=no --verbose --junit-xml=reports/results.xml --color=yes

log_cli = True
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_level = info

testpaths =
    .